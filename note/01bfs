前置认知：bfs和dfs的区别，就在于扩张集合是队列（bfs）还是栈（dfs）的区别

 01 bfs , 你如何在一个边权仅为0，1 的图中找到远点到每一个节点的最小距离？
 我们遇到0权边的时候，0权边是捷径，我们应该尽可能走捷径，所以针对此，我们要做dfs
 遇到其他边的时候，采用bfs ，但是一个集合，怎么同时打出stack和queue的双重效果？
 采用双端队列， 取得时候统一从头部取下， 若到下一个节点的weight为0 ， 头部插入。
 如果到下一个节点的weight为 1 ， 从尾部插入
 针对头部插入的0 weight节点， 头进，头出， 相当于stack ， dfs
 针对尾部插入的节点，尾进， 头出，相当于queue， bfs
 
 针对弹出的节点， 检查他的path有没有让已经记录的path更小，如果更小了， 就更新path

 另一种理解： 默认尾部进入， 一开始会忽略0 weight 边， 因此path一开始会恒大， 
 后续0 weight从头部插入后，会先弹出该节点，从而用0 区队原本不那么短的记录做一次【修正】
 所以遇到0的时候，我们从头部插入，目的是让期先弹出， 看看能不能修正已有结果

正统不变量理解：
0-1 BFS 是 Dijkstra 在 0/1 权下的一个特化实现


